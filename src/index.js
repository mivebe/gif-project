import React from "react";
import ReactDOM from "react-dom";
//import $ from "jquery"

import App from "./App.js";

ReactDOM.render(<App />, document.getElementById("root"));

/*
$("#nav").on("change", () => {
    if ($('#navInnerWrapper').height() > $('#nav').height()) {
        $('#nav').height($('#navInnerWrapper').height());
    }
});*/